# Story 3.3: Trade Lifecycle Management

## Status
Not Started

## Story
**As a** trader,
**I want** automatic management of my trades from entry to exit,
**so that** I can rely on systematic execution without manual intervention.

## Acceptance Criteria

1. Entry function monitored while position is flat (TradePlanStatus.awaiting_entry)
2. Upon entry trigger, position size calculated using existing risk management module
3. Risk checks performed before order submission (portfolio limit validation)
4. After successful risk validation and fill, stop-loss and take-profit orders placed immediately
5. Position added to risk registry with calculated risk amount upon fill
6. Exit functions (stop/target) monitored while position is open (TradePlanStatus.position_open)
7. Position state tracked through lifecycle: awaiting_entry → position_open → position_closed
8. Exit fills cancel any remaining orders for that symbol and remove position from risk registry

## Tasks / Subtasks

- [ ] **Task 1: Create Trade Orchestrator (AC: 1, 6, 7)**
  - [ ] Create `TradeOrchestrator` class to manage complete trade lifecycle
  - [ ] Implement state machine for trade plan status transitions
  - [ ] Add position monitoring with separate entry/exit function evaluation
  - [ ] Integrate with existing `ExecutionFunctionRegistry` from Stories 3.1-3.2
  - [ ] Add comprehensive unit tests for state transition logic

- [ ] **Task 2: Implement Entry Signal Processing (AC: 2, 3)**
  - [ ] Connect execution signals from Story 3.2 functions to orchestrator
  - [ ] Integrate with existing risk management system from Story 1.5.1
  - [ ] Add pre-trade portfolio limit validation using existing `RiskManager`
  - [ ] Implement position size calculation using established risk percentage logic
  - [ ] Add comprehensive testing for risk validation scenarios

- [ ] **Task 3: Implement Order Placement Logic (AC: 4, 5)**
  - [ ] Connect orchestrator to existing order execution system from Story 2.3
  - [ ] Implement bracket order placement (entry + stop-loss + take-profit)
  - [ ] Add position to risk registry upon successful fill confirmation
  - [ ] Integrate with existing `OrderExecutionManager` and `StateManager`
  - [ ] Add comprehensive testing for order placement scenarios

- [ ] **Task 4: Implement Exit Signal Processing (AC: 6, 8)**
  - [ ] Add exit function monitoring for open positions
  - [ ] Implement order cancellation logic upon exit signals
  - [ ] Remove position from risk registry upon exit fill
  - [ ] Update trade plan status to completed/cancelled appropriately
  - [ ] Add comprehensive testing for exit scenarios

- [ ] **Task 5: Add Integration Testing**
  - [ ] Create end-to-end integration tests for complete trade lifecycle
  - [ ] Test integration with all existing systems (risk, order, execution functions)
  - [ ] Add error handling tests for failed orders, disconnections, etc.
  - [ ] Performance testing for multiple simultaneous trade plans
  - [ ] Test edge cases: partial fills, rejected orders, market closures

## Dev Notes

### Previous Story Insights
**From Stories 3.1-3.2 completion (VERIFIED COMPLETED):**
- ExecutionFunctionBase abstract class and registry system fully implemented
- Three core execution functions (close_above, close_below, trailing_stop) operational
- BarCloseDetector with <1 second accuracy for signal generation
- Comprehensive execution logging and metrics system available
- Integration points ready for orchestration layer

**From Story 2.3 completion (VERIFIED COMPLETED):**
- Order execution interface with bracket order support implemented
- Risk validation integrated into order placement workflow
- State management system with atomic operations and backup rotation
- Order event handling system ready for lifecycle integration

**From Story 1.5.1 completion (VERIFIED COMPLETED):**
- Risk management system with portfolio limit validation
- Position sizing calculation with risk percentage (1%, 2%, 3%) options
- Risk registry tracking active positions and exposure
- Account balance validation and limit enforcement

### Data Models

**Existing Models Available (Dependencies):**
- `ExecutionSignal` - From Story 3.1, contains action and confidence score
- `ExecutionContext` - From Story 3.1, contains bar data and position state
- `TradePlan` - From Story 1.2, contains entry/exit levels and execution functions
- `TradePlanStatus` enum - States: awaiting_entry, position_open, completed, cancelled, error
- `Order` models - From Story 2.3, comprehensive order management
- `RiskCheck` - From Story 1.5.1, risk validation results
- `PositionState` - From Story 3.1, position tracking model

**New Models to Create (This Story):**
- `TradeLifecycleEvent` - Lifecycle transition events with timestamp and context
- `TradeOrchestrationConfig` - Configuration for orchestrator behavior
- `TradeCompletionSummary` - Summary of completed trades with P&L and metrics

### Component Integration Points

**ExecutionFunctionRegistry Integration** [Source: Story 3.1 completion]:
- Get function instance by name: `registry.get_function(function_name)`
- Evaluate function: `await function.evaluate(execution_context)`
- Built-in functions ready: CloseAboveFunction, CloseBelowFunction, TrailingStopFunction

**Risk Management Integration** [Source: Story 1.5.1 completion]:
- Portfolio limit validation: `risk_manager.validate_trade_request(trade_request)`
- Position size calculation: `risk_manager.calculate_position_size(account_balance, risk_percent, entry_price, stop_price)`
- Risk registry management: `risk_manager.add_position()` / `risk_manager.remove_position()`

**Order Execution Integration** [Source: Story 2.3 completion]:
- Bracket order placement: `order_manager.place_bracket_order(entry_order, stop_order, target_order)`
- Order fill monitoring: Subscribe to `OrderFillEvent` from order event system
- Order cancellation: `order_manager.cancel_orders_for_symbol(symbol)`

**Market Data Integration** [Source: Story 2.2 completion]:
- Bar data subscription: Subscribe to `BarData` events for execution function evaluation
- Market data cache access: `market_data_cache.get_latest_bar(symbol, timeframe)`

### Architecture Considerations

**State Machine Design:**
```python
class TradeLifecycleState(Enum):
    AWAITING_ENTRY = "awaiting_entry"
    POSITION_OPEN = "position_open" 
    POSITION_CLOSED = "position_closed"
    ERROR = "error"
    CANCELLED = "cancelled"

class TradeOrchestrator:
    async def process_market_data(self, bar_data: BarData) -> None:
        """Process bar data and evaluate appropriate functions based on trade state."""
        
    async def handle_entry_signal(self, signal: ExecutionSignal, trade_plan: TradePlan) -> None:
        """Handle entry signals with risk validation and order placement."""
        
    async def handle_exit_signal(self, signal: ExecutionSignal, trade_plan: TradePlan) -> None:
        """Handle exit signals with order cancellation and position cleanup."""
```

**Performance Requirements:**
- Signal to order submission latency: <1 second (per NFR1)
- Support multiple simultaneous trade plans
- Handle high-frequency bar data updates efficiently

**Error Handling Strategy:**
- Comprehensive logging of all lifecycle events using existing ExecutionLogger
- Graceful degradation for non-critical failures
- Position state recovery for system restart scenarios (Story 4.3 dependency)

**Testing Strategy:**
- Unit tests for each orchestrator method (80% coverage minimum)
- Integration tests with mock IBKR client for complete workflows
- Performance tests for latency requirements
- Chaos testing for error scenarios and edge cases

### Implementation Priority

1. **Core Orchestrator** - State machine and lifecycle management
2. **Entry Processing** - Signal handling and risk validation integration  
3. **Order Placement** - Bracket order integration with existing systems
4. **Exit Processing** - Exit signal handling and position cleanup
5. **Integration Testing** - End-to-end workflow validation

### Success Criteria

- Complete trades execute from entry signal to exit within system latency requirements
- All existing risk management rules properly enforced during lifecycle
- Position state accurately tracked and reported throughout lifecycle
- System handles multiple simultaneous trades without interference
- Integration with all existing systems (Stories 1.x, 2.x, 3.1-3.2) maintains their functionality